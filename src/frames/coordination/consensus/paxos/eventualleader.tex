% SPDX-License-Identifier: CC-BY-SA-4.0
% Author: Matthieu Perrin
% Part: 
% Section: 
% Sub-section: 
% Frame: 

\begingroup

\SetKwFunction{Leader}{leader}

\begin{frame}[fragile]{Un leader à terme}
  
  \onBlock[top]{Définition -- Leader à terme $\Omega$}{
    \begin{algorithm}[H]
      \Interface{$\Omega$}{
        \lMethod{$\Leader()$}{\tcp*[f]{retourne un identifiant de processus}}
      }
    \end{algorithm}
    \begin{itemize}
    \item $\Leader()$ \alert{finira par} retourner \structure{toujours le même} processus \structure{correct}
    \end{itemize}
  }

  \on[y=-16mm]{
    \begin{tikzpicture}[y=8mm, x=10mm]
      \draw[crashed] (0,3) node[left]{$p_1$} to (2,3); 
      \draw[process] (0,2) node[left, alert]{$p_2$} to (10,2); 
      \draw[process] (0,1) node[left, example]{$p_3$} to (10,1); 
      \draw[process] (0,0) node[left, structure]{$p_4$} to (10,0);

      \scriptsize
      \node[fade,      operation] at (1.3,2) {$\Leader() \rightarrow 1$};
      \node[alert,     operation] at (1.7,1) {$\Leader() \rightarrow 2$};
      \node[example,   operation] at (1.5,0) {$\Leader() \rightarrow 3$};
      \node[alert,     operation] at (3.9,2) {$\Leader() \rightarrow 2$};
      \node[structure, operation] at (3.8,1) {$\Leader() \rightarrow 4$};
      \node[fade,      operation] at (3.5,0) {$\Leader() \rightarrow 1$};
      
      \node[example,   operation] at (6.3,2) {$\Leader() \rightarrow 3$};
      \node[example,   operation] at (6.5,1) {$\Leader() \rightarrow 3$};
      \node[example,   operation] at (6.1,0) {$\Leader() \rightarrow 3$};
      \node[example,   operation] at (8.4,2) {$\Leader() \rightarrow 3$};
      \node[example,   operation] at (8.5,1) {$\Leader() \rightarrow 3$};
      \node[example,   operation] at (8.7,0) {$\Leader() \rightarrow 3$};

      \normalsize
      \draw[double] (5,-.5) -- (5,2.5);
      \path (5 ,-.5) edge[brace] node[align=center]{Avant la stabilisation \\ \alert{Comment maintenir la sûreté ?}} (0,-.5);
      \path (10,-.5) edge[brace] node[align=center]{Après la stabilisation \\ \alert{comment assurer la vivacité ?}} (5,-.5);
    \end{tikzpicture}
  }

  \footnoteref{T. Chandra, V. Hadzilacos, S. Toueg. \textit{The weakest failure detector for solving consensus.} JACM (1996)}
  
\end{frame}

\endgroup
\endinput
